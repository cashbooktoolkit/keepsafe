<%= form_for(@storeditem, html: {multipart: true} ) do |f| %>
  <% if @storeditem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@storeditem.errors.count, "error") %> prohibited this storeditem from being saved:</h2>

      <ul>
      <% @storeditem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :document, "File" %><br>
     <%= f.file_field :document %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
